on:
  push:
jobs:
  log-group:
    runs-on: ubuntu-latest
    steps:
      - run:  echo "::group::ecole-it"
              echo "hello, ecole-it"
              echo "::endgroup::"
              echo "ceci n'est pas dans le groupe"
